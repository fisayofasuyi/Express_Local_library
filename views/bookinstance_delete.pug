extends layout 

block content

  h1 #{title}

  h3 Book instance Details:

  p #[strong Id]: #{bookinstances._id} 
  p #[strong Imprint]: #{bookinstances.imprint} 
  p #[strong Status]: #{bookinstances.status}
  
  P Are you sere you want to delete this book instance?

  form(method='POST' action='')
         div.form-group
           input#bookinstanceid.form-control(type='hidden', name='bookinstanceid', required='true', value=bookinstances._id)

         button.btn.btn-primary(type='submit') Delete
